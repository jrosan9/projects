{
  "name": "event_ticketer",
  "version": "1.0.0",
  "main": "./db/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "npx prisma migrate reset --force && node ./db/seed.js",
    "start:dev": " nodemon server.js",
    "start_all": "cd client && npm run build && cd .. && npm run start:dev "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prisma": "^5.16.1"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2"
  }
}
